export default class Scene1 {
    constructor(experience) {
        this.experience = experience;
        this.scene = experience.scene;
    }

    update(progress, time) {
        // Scene 1 - Compassionate Listening
    }

    destroy() {}
}
